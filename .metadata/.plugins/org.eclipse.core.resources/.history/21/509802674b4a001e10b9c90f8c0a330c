#include "start.h"
#include "admin.h"
#include "user.h"

node *head=NULL;
void start_system(void)
{
	add_new_patient(&head);
	add_new_patient(&head);
	add_new_patient(&head);
	edit_patient(head);
	print_patient_record(head);

}
